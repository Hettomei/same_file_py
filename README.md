# Hos to start

```
python main.py --help

usage: Same file [-h] [--feed-with ROOT_PATH] [--show-same-files]

Find same file based on sha256

options:
  -h, --help            show this help message and exit
  --feed-with ROOT_PATH
                        Root path for computing all sha256 digest
  --show-same-files     Explain which file are duplicated
```
